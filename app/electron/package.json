{
  "name": "hotel-electron",
  "version": "1.0.0",
  "private": true,
  "main": "main.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development FRONTEND_URL=http://localhost:5173 BACKEND_URL=http://localhost:3001 wait-on tcp:5173 tcp:3001 && electron .",
    "start": "cross-env NODE_ENV=production FRONTEND_URL=http://localhost:4173 BACKEND_URL=http://localhost:3001 wait-on tcp:4173 tcp:3001 && electron ."
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "electron": "^35.1.5",
    "wait-on": "^8.0.3"
  }
}
